# Alpha ADK

Luke's personal workspace. Fork of `nextain-adk`.

## Upstream

```
origin:   nextain/alpha-adk
upstream: nextain/nextain-adk
```

Sync: `git fetch upstream && git merge upstream/main`

## Projects

See `.gitmodules` for full list.

## Default Language

Korean (`ko`) for `.users/` mirror.
