# session-manifest.yaml — 최소 예제 (24h 야간 T-B 세션 환산)

version: 1

id: T-B-prod-investigation
bound_project: projects/onmam-dev

persona:
  base: coder
  extra_system_prompt: |
    prod 조사·계획·spec 공급 lane. worktree/origin/dev-server mutate 금지.
    Track A 8라운드 over-claim 교훈: advisory · mitigation · semi-automatic 언어만.

capability_profile: read-mostly-with-spec-write

llm:
  provider: anthropic
  model: claude-opus-4-7-1m
  cost_budget_ref: budget-personal-2026-05

ephemeral_paths:
  - ".agents/progress/onmam-prod-pulldown-2026-05-20.md"
  - ".agents/progress/onmam-night-mutation.lock"

started_at: "2026-05-20T00:30:00Z"

notes: |
  24시간 onmam 야간 실험의 T-B 세션. dev-side 가 worktree/origin 오너,
  Track A 가 service-ops 프레임 lane, T-B 가 prod read-only 조사 + spec 공급 lane.
  본 manifest 는 그 lane assignment 를 artifact 화한 예제.
