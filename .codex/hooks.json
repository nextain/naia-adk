{
  "hooks": {
    "PreToolUse": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "root=${ADK_PROJECT_ROOT:-}; if [ -n \"$root\" ]; then case \"$root\" in /*) ;; *) exit 1;; esac; root=$(CDPATH= cd -- \"$root\" 2>/dev/null && pwd -P) || exit 1; else root=$(git rev-parse --show-toplevel 2>/dev/null) || exit 1; fi; [ -f \"$root/.codex/hooks.json\" ] || exit 1; registry=\"$root/.codex/hooks.json\"; [ ! -f \"$registry\" ] && exit 0; hook=\"$root/.codex/hooks/context-budget-guard.cjs\"; [ ! -f \"$hook\" ] || node \"$hook\"",
            "commandWindows": "powershell -NoProfile -Command '$root=$env:ADK_PROJECT_ROOT; if ($root) { if (-not [IO.Path]::IsPathRooted($root)) { exit 1 }; try { $root=(Resolve-Path -LiteralPath $root -ErrorAction Stop).Path } catch { exit 1 } } else { $root=git rev-parse --show-toplevel 2>$null; if ($LASTEXITCODE -ne 0 -or -not $root) { exit 1 }; $root=$root.Trim() }; if (-not (Test-Path -LiteralPath (Join-Path $root \".codex/hooks.json\"))) { exit 1 }; $registry=Join-Path $root.Trim() \".codex/hooks.json\"; if (-not (Test-Path -LiteralPath $registry)) { exit 0 }; $hook=Join-Path $root.Trim() \".codex/hooks/context-budget-guard.cjs\"; if (Test-Path -LiteralPath $hook) { node $hook }'",
            "statusMessage": "Checking retained context budget"
          }
        ]
      },
      {
        "matcher": "Bash|shell_command|exec_command|(?:.*[.:/]exec_command)|(?:.*[.:/]shell_command)|Edit|Write|NotebookEdit|apply_patch",
        "hooks": [
          {
            "type": "command",
            "command": "root=${ADK_PROJECT_ROOT:-}; if [ -n \"$root\" ]; then case \"$root\" in /*) ;; *) exit 1;; esac; root=$(CDPATH= cd -- \"$root\" 2>/dev/null && pwd -P) || exit 1; else root=$(git rev-parse --show-toplevel 2>/dev/null) || exit 1; fi; [ -f \"$root/.codex/hooks.json\" ] || exit 1; registry=\"$root/.codex/hooks.json\"; [ ! -f \"$registry\" ] && exit 0; hook=\"$root/.codex/hooks/session-contract-gate.cjs\"; if [ ! -f \"$hook\" ]; then echo \"Configured Codex hook is missing: $hook\" >&2; exit 1; fi; node \"$hook\"",
            "commandWindows": "powershell -NoProfile -Command '$root=$env:ADK_PROJECT_ROOT; if ($root) { if (-not [IO.Path]::IsPathRooted($root)) { exit 1 }; try { $root=(Resolve-Path -LiteralPath $root -ErrorAction Stop).Path } catch { exit 1 } } else { $root=git rev-parse --show-toplevel 2>$null; if ($LASTEXITCODE -ne 0 -or -not $root) { exit 1 }; $root=$root.Trim() }; if (-not (Test-Path -LiteralPath (Join-Path $root \".codex/hooks.json\"))) { exit 1 }; $registry=Join-Path $root.Trim() \".codex/hooks.json\"; if (-not (Test-Path -LiteralPath $registry)) { exit 0 }; $hook=Join-Path $root.Trim() \".codex/hooks/session-contract-gate.cjs\"; if (-not (Test-Path -LiteralPath $hook)) { Write-Error \"Configured Codex hook is missing: $hook\"; exit 1 }; node $hook'",
            "statusMessage": "Checking ADK session contract"
          }
        ]
      },
      {
        "matcher": "Bash|shell_command|exec_command|(?:.*[.:/]exec_command)|(?:.*[.:/]shell_command)|Edit|Write|NotebookEdit|apply_patch",
        "hooks": [
          {
            "type": "command",
            "command": "root=${ADK_PROJECT_ROOT:-}; if [ -n \"$root\" ]; then case \"$root\" in /*) ;; *) exit 1;; esac; root=$(CDPATH= cd -- \"$root\" 2>/dev/null && pwd -P) || exit 1; else root=$(git rev-parse --show-toplevel 2>/dev/null) || exit 1; fi; [ -f \"$root/.codex/hooks.json\" ] || exit 1; registry=\"$root/.codex/hooks.json\"; [ ! -f \"$registry\" ] && exit 0; hook=\"$root/.codex/hooks/request-contract.cjs\"; if [ ! -f \"$hook\" ]; then echo \"Configured Codex hook is missing: $hook\" >&2; exit 1; fi; node \"$hook\" PreToolUse",
            "commandWindows": "powershell -NoProfile -Command '$root=$env:ADK_PROJECT_ROOT; if ($root) { if (-not [IO.Path]::IsPathRooted($root)) { exit 1 }; try { $root=(Resolve-Path -LiteralPath $root -ErrorAction Stop).Path } catch { exit 1 } } else { $root=git rev-parse --show-toplevel 2>$null; if ($LASTEXITCODE -ne 0 -or -not $root) { exit 1 }; $root=$root.Trim() }; if (-not (Test-Path -LiteralPath (Join-Path $root \".codex/hooks.json\"))) { exit 1 }; $registry=Join-Path $root.Trim() \".codex/hooks.json\"; if (-not (Test-Path -LiteralPath $registry)) { exit 0 }; $hook=Join-Path $root.Trim() \".codex/hooks/request-contract.cjs\"; if (-not (Test-Path -LiteralPath $hook)) { Write-Error \"Configured Codex hook is missing: $hook\"; exit 1 }; node $hook PreToolUse'"
          }
        ]
      },
      {
        "hooks": [
          {
            "type": "command",
            "command": "root=${ADK_PROJECT_ROOT:-}; if [ -n \"$root\" ]; then case \"$root\" in /*) ;; *) exit 1;; esac; root=$(CDPATH= cd -- \"$root\" 2>/dev/null && pwd -P) || exit 1; else root=$(git rev-parse --show-toplevel 2>/dev/null) || exit 1; fi; [ -f \"$root/.codex/hooks.json\" ] || exit 1; registry=\"$root/.codex/hooks.json\"; [ ! -f \"$registry\" ] && exit 0; hook=\"$root/.codex/hooks/subagent-spawn-guard.cjs\"; if [ ! -f \"$hook\" ]; then echo \"Configured Codex hook is missing: $hook\" >&2; exit 1; fi; node \"$hook\"",
            "commandWindows": "powershell -NoProfile -Command '$root=$env:ADK_PROJECT_ROOT; if ($root) { if (-not [IO.Path]::IsPathRooted($root)) { exit 1 }; try { $root=(Resolve-Path -LiteralPath $root -ErrorAction Stop).Path } catch { exit 1 } } else { $root=git rev-parse --show-toplevel 2>$null; if ($LASTEXITCODE -ne 0 -or -not $root) { exit 1 }; $root=$root.Trim() }; if (-not (Test-Path -LiteralPath (Join-Path $root \".codex/hooks.json\"))) { exit 1 }; $registry=Join-Path $root.Trim() \".codex/hooks.json\"; if (-not (Test-Path -LiteralPath $registry)) { exit 0 }; $hook=Join-Path $root.Trim() \".codex/hooks/subagent-spawn-guard.cjs\"; if (-not (Test-Path -LiteralPath $hook)) { Write-Error \"Configured Codex hook is missing: $hook\"; exit 1 }; node $hook'",
            "statusMessage": "Checking subagent spawn budget"
          }
        ]
      },
      {
        "hooks": [
          {
            "type": "command",
            "command": "root=${ADK_PROJECT_ROOT:-}; if [ -n \"$root\" ]; then case \"$root\" in /*) ;; *) exit 1;; esac; root=$(CDPATH= cd -- \"$root\" 2>/dev/null && pwd -P) || exit 1; else root=$(git rev-parse --show-toplevel 2>/dev/null) || exit 1; fi; [ -f \"$root/.codex/hooks.json\" ] || exit 1; registry=\"$root/.codex/hooks.json\"; [ ! -f \"$registry\" ] && exit 0; hook=\"$root/.codex/hooks/codex-cost-guard.cjs\"; if [ ! -f \"$hook\" ]; then echo \"Configured Codex hook is missing: $hook\" >&2; exit 1; fi; node \"$hook\"",
            "commandWindows": "powershell -NoProfile -Command '$root=$env:ADK_PROJECT_ROOT; if ($root) { if (-not [IO.Path]::IsPathRooted($root)) { exit 1 }; try { $root=(Resolve-Path -LiteralPath $root -ErrorAction Stop).Path } catch { exit 1 } } else { $root=git rev-parse --show-toplevel 2>$null; if ($LASTEXITCODE -ne 0 -or -not $root) { exit 1 }; $root=$root.Trim() }; if (-not (Test-Path -LiteralPath (Join-Path $root \".codex/hooks.json\"))) { exit 1 }; $registry=Join-Path $root.Trim() \".codex/hooks.json\"; if (-not (Test-Path -LiteralPath $registry)) { exit 0 }; $hook=Join-Path $root.Trim() \".codex/hooks/codex-cost-guard.cjs\"; if (-not (Test-Path -LiteralPath $hook)) { Write-Error \"Configured Codex hook is missing: $hook\"; exit 1 }; node $hook'",
            "statusMessage": "Checking Codex lineage cost"
          },
          {
            "type": "command",
            "command": "root=${ADK_PROJECT_ROOT:-}; if [ -n \"$root\" ]; then case \"$root\" in /*) ;; *) exit 1;; esac; root=$(CDPATH= cd -- \"$root\" 2>/dev/null && pwd -P) || exit 1; else root=$(git rev-parse --show-toplevel 2>/dev/null) || exit 1; fi; [ -f \"$root/.codex/hooks.json\" ] || exit 1; registry=\"$root/.codex/hooks.json\"; [ ! -f \"$registry\" ] && exit 0; watchdog=\"$root/scripts/codex-cost-watchdog.mjs\"; if [ ! -f \"$watchdog\" ]; then echo \"Configured Codex watchdog is missing: $watchdog\" >&2; exit 1; fi; node \"$watchdog\" --enforce --ensure-watch >/dev/null",
            "commandWindows": "powershell -NoProfile -Command '$root=$env:ADK_PROJECT_ROOT; if ($root) { if (-not [IO.Path]::IsPathRooted($root)) { exit 1 }; try { $root=(Resolve-Path -LiteralPath $root -ErrorAction Stop).Path } catch { exit 1 } } else { $root=git rev-parse --show-toplevel 2>$null; if ($LASTEXITCODE -ne 0 -or -not $root) { exit 1 }; $root=$root.Trim() }; if (-not (Test-Path -LiteralPath (Join-Path $root \".codex/hooks.json\"))) { exit 1 }; $watchdog=Join-Path $root \"scripts/codex-cost-watchdog.mjs\"; if (-not (Test-Path -LiteralPath $watchdog)) { Write-Error \"Configured Codex watchdog is missing: $watchdog\"; exit 1 }; node $watchdog --enforce --ensure-watch | Out-Null'",
            "statusMessage": "Enforcing Codex descendant cost ceilings"
          }
        ]
      }
    ],
    "SessionStart": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "root=${ADK_PROJECT_ROOT:-}; if [ -n \"$root\" ]; then case \"$root\" in /*) ;; *) exit 1;; esac; root=$(CDPATH= cd -- \"$root\" 2>/dev/null && pwd -P) || exit 1; else root=$(git rev-parse --show-toplevel 2>/dev/null) || exit 1; fi; [ -f \"$root/.codex/hooks.json\" ] || exit 1; registry=\"$root/.codex/hooks.json\"; [ ! -f \"$registry\" ] && exit 0; hook=\"$root/.codex/hooks/session-inject.cjs\"; if [ ! -f \"$hook\" ]; then echo \"Configured Codex hook is missing: $hook\" >&2; exit 1; fi; node \"$hook\"",
            "commandWindows": "powershell -NoProfile -Command '$root=$env:ADK_PROJECT_ROOT; if ($root) { if (-not [IO.Path]::IsPathRooted($root)) { exit 1 }; try { $root=(Resolve-Path -LiteralPath $root -ErrorAction Stop).Path } catch { exit 1 } } else { $root=git rev-parse --show-toplevel 2>$null; if ($LASTEXITCODE -ne 0 -or -not $root) { exit 1 }; $root=$root.Trim() }; if (-not (Test-Path -LiteralPath (Join-Path $root \".codex/hooks.json\"))) { exit 1 }; $registry=Join-Path $root.Trim() \".codex/hooks.json\"; if (-not (Test-Path -LiteralPath $registry)) { exit 0 }; $hook=Join-Path $root.Trim() \".codex/hooks/session-inject.cjs\"; if (-not (Test-Path -LiteralPath $hook)) { Write-Error \"Configured Codex hook is missing: $hook\"; exit 1 }; node $hook'",
            "statusMessage": "Loading ADK workflow state"
          },
          {
            "type": "command",
            "command": "root=${ADK_PROJECT_ROOT:-}; if [ -n \"$root\" ]; then case \"$root\" in /*) ;; *) exit 1;; esac; root=$(CDPATH= cd -- \"$root\" 2>/dev/null && pwd -P) || exit 1; else root=$(git rev-parse --show-toplevel 2>/dev/null) || exit 1; fi; [ -f \"$root/.codex/hooks.json\" ] || exit 1; registry=\"$root/.codex/hooks.json\"; [ ! -f \"$registry\" ] && exit 0; hook=\"$root/.codex/hooks/request-contract.cjs\"; if [ ! -f \"$hook\" ]; then echo \"Configured Codex hook is missing: $hook\" >&2; exit 1; fi; node \"$hook\" SessionStart",
            "commandWindows": "powershell -NoProfile -Command '$root=$env:ADK_PROJECT_ROOT; if ($root) { if (-not [IO.Path]::IsPathRooted($root)) { exit 1 }; try { $root=(Resolve-Path -LiteralPath $root -ErrorAction Stop).Path } catch { exit 1 } } else { $root=git rev-parse --show-toplevel 2>$null; if ($LASTEXITCODE -ne 0 -or -not $root) { exit 1 }; $root=$root.Trim() }; if (-not (Test-Path -LiteralPath (Join-Path $root \".codex/hooks.json\"))) { exit 1 }; $registry=Join-Path $root.Trim() \".codex/hooks.json\"; if (-not (Test-Path -LiteralPath $registry)) { exit 0 }; $hook=Join-Path $root.Trim() \".codex/hooks/request-contract.cjs\"; if (-not (Test-Path -LiteralPath $hook)) { Write-Error \"Configured Codex hook is missing: $hook\"; exit 1 }; node $hook SessionStart'"
          }
        ]
      }
    ],
    "UserPromptSubmit": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "root=${ADK_PROJECT_ROOT:-}; if [ -n \"$root\" ]; then case \"$root\" in /*) ;; *) exit 1;; esac; root=$(CDPATH= cd -- \"$root\" 2>/dev/null && pwd -P) || exit 1; else root=$(git rev-parse --show-toplevel 2>/dev/null) || exit 1; fi; [ -f \"$root/.codex/hooks.json\" ] || exit 1; registry=\"$root/.codex/hooks.json\"; [ ! -f \"$registry\" ] && exit 0; hook=\"$root/.codex/hooks/session-inject.cjs\"; if [ ! -f \"$hook\" ]; then echo \"Configured Codex hook is missing: $hook\" >&2; exit 1; fi; node \"$hook\"",
            "commandWindows": "powershell -NoProfile -Command '$root=$env:ADK_PROJECT_ROOT; if ($root) { if (-not [IO.Path]::IsPathRooted($root)) { exit 1 }; try { $root=(Resolve-Path -LiteralPath $root -ErrorAction Stop).Path } catch { exit 1 } } else { $root=git rev-parse --show-toplevel 2>$null; if ($LASTEXITCODE -ne 0 -or -not $root) { exit 1 }; $root=$root.Trim() }; if (-not (Test-Path -LiteralPath (Join-Path $root \".codex/hooks.json\"))) { exit 1 }; $registry=Join-Path $root.Trim() \".codex/hooks.json\"; if (-not (Test-Path -LiteralPath $registry)) { exit 0 }; $hook=Join-Path $root.Trim() \".codex/hooks/session-inject.cjs\"; if (-not (Test-Path -LiteralPath $hook)) { Write-Error \"Configured Codex hook is missing: $hook\"; exit 1 }; node $hook'",
            "statusMessage": "Refreshing ADK workflow state"
          },
          {
            "type": "command",
            "command": "root=${ADK_PROJECT_ROOT:-}; if [ -n \"$root\" ]; then case \"$root\" in /*) ;; *) exit 1;; esac; root=$(CDPATH= cd -- \"$root\" 2>/dev/null && pwd -P) || exit 1; else root=$(git rev-parse --show-toplevel 2>/dev/null) || exit 1; fi; [ -f \"$root/.codex/hooks.json\" ] || exit 1; registry=\"$root/.codex/hooks.json\"; [ ! -f \"$registry\" ] && exit 0; hook=\"$root/.codex/hooks/request-contract.cjs\"; if [ ! -f \"$hook\" ]; then echo \"Configured Codex hook is missing: $hook\" >&2; exit 1; fi; node \"$hook\" UserPromptSubmit",
            "commandWindows": "powershell -NoProfile -Command '$root=$env:ADK_PROJECT_ROOT; if ($root) { if (-not [IO.Path]::IsPathRooted($root)) { exit 1 }; try { $root=(Resolve-Path -LiteralPath $root -ErrorAction Stop).Path } catch { exit 1 } } else { $root=git rev-parse --show-toplevel 2>$null; if ($LASTEXITCODE -ne 0 -or -not $root) { exit 1 }; $root=$root.Trim() }; if (-not (Test-Path -LiteralPath (Join-Path $root \".codex/hooks.json\"))) { exit 1 }; $registry=Join-Path $root.Trim() \".codex/hooks.json\"; if (-not (Test-Path -LiteralPath $registry)) { exit 0 }; $hook=Join-Path $root.Trim() \".codex/hooks/request-contract.cjs\"; if (-not (Test-Path -LiteralPath $hook)) { Write-Error \"Configured Codex hook is missing: $hook\"; exit 1 }; node $hook UserPromptSubmit'"
          },
          {
            "type": "command",
            "command": "root=${ADK_PROJECT_ROOT:-}; if [ -n \"$root\" ]; then case \"$root\" in /*) ;; *) exit 1;; esac; root=$(CDPATH= cd -- \"$root\" 2>/dev/null && pwd -P) || exit 1; else root=$(git rev-parse --show-toplevel 2>/dev/null) || exit 1; fi; [ -f \"$root/.codex/hooks.json\" ] || exit 1; registry=\"$root/.codex/hooks.json\"; [ ! -f \"$registry\" ] && exit 0; hook=\"$root/.codex/hooks/codex-cost-guard.cjs\"; if [ ! -f \"$hook\" ]; then echo \"Configured Codex hook is missing: $hook\" >&2; exit 1; fi; node \"$hook\"",
            "commandWindows": "powershell -NoProfile -Command '$root=$env:ADK_PROJECT_ROOT; if ($root) { if (-not [IO.Path]::IsPathRooted($root)) { exit 1 }; try { $root=(Resolve-Path -LiteralPath $root -ErrorAction Stop).Path } catch { exit 1 } } else { $root=git rev-parse --show-toplevel 2>$null; if ($LASTEXITCODE -ne 0 -or -not $root) { exit 1 }; $root=$root.Trim() }; if (-not (Test-Path -LiteralPath (Join-Path $root \".codex/hooks.json\"))) { exit 1 }; $registry=Join-Path $root.Trim() \".codex/hooks.json\"; if (-not (Test-Path -LiteralPath $registry)) { exit 0 }; $hook=Join-Path $root.Trim() \".codex/hooks/codex-cost-guard.cjs\"; if (-not (Test-Path -LiteralPath $hook)) { Write-Error \"Configured Codex hook is missing: $hook\"; exit 1 }; node $hook'",
            "statusMessage": "Checking Codex lineage cost"
          },
          {
            "type": "command",
            "command": "root=${ADK_PROJECT_ROOT:-}; if [ -n \"$root\" ]; then case \"$root\" in /*) ;; *) exit 1;; esac; root=$(CDPATH= cd -- \"$root\" 2>/dev/null && pwd -P) || exit 1; else root=$(git rev-parse --show-toplevel 2>/dev/null) || exit 1; fi; [ -f \"$root/.codex/hooks.json\" ] || exit 1; registry=\"$root/.codex/hooks.json\"; [ ! -f \"$registry\" ] && exit 0; watchdog=\"$root/scripts/codex-cost-watchdog.mjs\"; if [ ! -f \"$watchdog\" ]; then echo \"Configured Codex watchdog is missing: $watchdog\" >&2; exit 1; fi; node \"$watchdog\" --enforce --ensure-watch >/dev/null",
            "commandWindows": "powershell -NoProfile -Command '$root=$env:ADK_PROJECT_ROOT; if ($root) { if (-not [IO.Path]::IsPathRooted($root)) { exit 1 }; try { $root=(Resolve-Path -LiteralPath $root -ErrorAction Stop).Path } catch { exit 1 } } else { $root=git rev-parse --show-toplevel 2>$null; if ($LASTEXITCODE -ne 0 -or -not $root) { exit 1 }; $root=$root.Trim() }; if (-not (Test-Path -LiteralPath (Join-Path $root \".codex/hooks.json\"))) { exit 1 }; $watchdog=Join-Path $root \"scripts/codex-cost-watchdog.mjs\"; if (-not (Test-Path -LiteralPath $watchdog)) { Write-Error \"Configured Codex watchdog is missing: $watchdog\"; exit 1 }; node $watchdog --enforce --ensure-watch | Out-Null'",
            "statusMessage": "Enforcing Codex descendant cost ceilings"
          }
        ]
      }
    ],
    "PostToolUse": [
      {
        "matcher": "Edit|Write|apply_patch",
        "hooks": [
          {
            "type": "command",
            "command": "root=${ADK_PROJECT_ROOT:-}; if [ -n \"$root\" ]; then case \"$root\" in /*) ;; *) exit 1;; esac; root=$(CDPATH= cd -- \"$root\" 2>/dev/null && pwd -P) || exit 1; else root=$(git rev-parse --show-toplevel 2>/dev/null) || exit 1; fi; [ -f \"$root/.codex/hooks.json\" ] || exit 1; hook=\"$root/.claude/hooks/sync-entry-points.js\"; [ ! -f \"$hook\" ] || node \"$hook\"",
            "commandWindows": "powershell -NoProfile -Command '$root=$env:ADK_PROJECT_ROOT; if ($root) { if (-not [IO.Path]::IsPathRooted($root)) { exit 1 }; try { $root=(Resolve-Path -LiteralPath $root -ErrorAction Stop).Path } catch { exit 1 } } else { $root=git rev-parse --show-toplevel 2>$null; if ($LASTEXITCODE -ne 0 -or -not $root) { exit 1 }; $root=$root.Trim() }; if (-not (Test-Path -LiteralPath (Join-Path $root \".codex/hooks.json\"))) { exit 1 }; $hook=Join-Path $root.Trim() \".claude/hooks/sync-entry-points.js\"; if (-not (Test-Path -LiteralPath $hook)) { exit 0 }; node $hook'"
          },
          {
            "type": "command",
            "command": "root=${ADK_PROJECT_ROOT:-}; if [ -n \"$root\" ]; then case \"$root\" in /*) ;; *) exit 1;; esac; root=$(CDPATH= cd -- \"$root\" 2>/dev/null && pwd -P) || exit 1; else root=$(git rev-parse --show-toplevel 2>/dev/null) || exit 1; fi; [ -f \"$root/.codex/hooks.json\" ] || exit 1; hook=\"$root/.claude/hooks/context-translation-batch.cjs\"; [ ! -f \"$hook\" ] || node \"$hook\"",
            "commandWindows": "powershell -NoProfile -Command '$root=$env:ADK_PROJECT_ROOT; if ($root) { if (-not [IO.Path]::IsPathRooted($root)) { exit 1 }; try { $root=(Resolve-Path -LiteralPath $root -ErrorAction Stop).Path } catch { exit 1 } } else { $root=git rev-parse --show-toplevel 2>$null; if ($LASTEXITCODE -ne 0 -or -not $root) { exit 1 }; $root=$root.Trim() }; if (-not (Test-Path -LiteralPath (Join-Path $root \".codex/hooks.json\"))) { exit 1 }; $hook=Join-Path $root.Trim() \".claude/hooks/context-translation-batch.cjs\"; if (-not (Test-Path -LiteralPath $hook)) { exit 0 }; node $hook'"
          }
        ]
      },
      {
        "hooks": [
          {
            "type": "command",
            "command": "root=${ADK_PROJECT_ROOT:-}; if [ -n \"$root\" ]; then case \"$root\" in /*) ;; *) exit 1;; esac; root=$(CDPATH= cd -- \"$root\" 2>/dev/null && pwd -P) || exit 1; else root=$(git rev-parse --show-toplevel 2>/dev/null) || exit 1; fi; [ -f \"$root/.codex/hooks.json\" ] || exit 1; registry=\"$root/.codex/hooks.json\"; [ ! -f \"$registry\" ] && exit 0; hook=\"$root/.codex/hooks/subagent-spawn-guard.cjs\"; if [ ! -f \"$hook\" ]; then echo \"Configured Codex hook is missing: $hook\" >&2; exit 1; fi; node \"$hook\"",
            "commandWindows": "powershell -NoProfile -Command '$root=$env:ADK_PROJECT_ROOT; if ($root) { if (-not [IO.Path]::IsPathRooted($root)) { exit 1 }; try { $root=(Resolve-Path -LiteralPath $root -ErrorAction Stop).Path } catch { exit 1 } } else { $root=git rev-parse --show-toplevel 2>$null; if ($LASTEXITCODE -ne 0 -or -not $root) { exit 1 }; $root=$root.Trim() }; if (-not (Test-Path -LiteralPath (Join-Path $root \".codex/hooks.json\"))) { exit 1 }; $registry=Join-Path $root.Trim() \".codex/hooks.json\"; if (-not (Test-Path -LiteralPath $registry)) { exit 0 }; $hook=Join-Path $root.Trim() \".codex/hooks/subagent-spawn-guard.cjs\"; if (-not (Test-Path -LiteralPath $hook)) { Write-Error \"Configured Codex hook is missing: $hook\"; exit 1 }; node $hook'",
            "statusMessage": "Checking subagent result contract"
          },
          {
            "type": "command",
            "command": "root=${ADK_PROJECT_ROOT:-}; if [ -n \"$root\" ]; then case \"$root\" in /*) ;; *) exit 1;; esac; root=$(CDPATH= cd -- \"$root\" 2>/dev/null && pwd -P) || exit 1; else root=$(git rev-parse --show-toplevel 2>/dev/null) || exit 1; fi; [ -f \"$root/.codex/hooks.json\" ] || exit 1; registry=\"$root/.codex/hooks.json\"; [ ! -f \"$registry\" ] && exit 0; hook=\"$root/.codex/hooks/request-contract.cjs\"; if [ ! -f \"$hook\" ]; then echo \"Configured Codex hook is missing: $hook\" >&2; exit 1; fi; node \"$hook\" PostToolUse",
            "commandWindows": "powershell -NoProfile -Command '$root=$env:ADK_PROJECT_ROOT; if ($root) { if (-not [IO.Path]::IsPathRooted($root)) { exit 1 }; try { $root=(Resolve-Path -LiteralPath $root -ErrorAction Stop).Path } catch { exit 1 } } else { $root=git rev-parse --show-toplevel 2>$null; if ($LASTEXITCODE -ne 0 -or -not $root) { exit 1 }; $root=$root.Trim() }; if (-not (Test-Path -LiteralPath (Join-Path $root \".codex/hooks.json\"))) { exit 1 }; $registry=Join-Path $root.Trim() \".codex/hooks.json\"; if (-not (Test-Path -LiteralPath $registry)) { exit 0 }; $hook=Join-Path $root.Trim() \".codex/hooks/request-contract.cjs\"; if (-not (Test-Path -LiteralPath $hook)) { Write-Error \"Configured Codex hook is missing: $hook\"; exit 1 }; node $hook PostToolUse'"
          }
        ]
      }
    ],
    "PreCompact": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "root=${ADK_PROJECT_ROOT:-}; if [ -n \"$root\" ]; then case \"$root\" in /*) ;; *) exit 1;; esac; root=$(CDPATH= cd -- \"$root\" 2>/dev/null && pwd -P) || exit 1; else root=$(git rev-parse --show-toplevel 2>/dev/null) || exit 1; fi; [ -f \"$root/.codex/hooks.json\" ] || exit 1; registry=\"$root/.codex/hooks.json\"; [ ! -f \"$registry\" ] && exit 0; hook=\"$root/.codex/hooks/request-contract.cjs\"; if [ ! -f \"$hook\" ]; then echo \"Configured Codex hook is missing: $hook\" >&2; exit 1; fi; node \"$hook\" PreCompact",
            "commandWindows": "powershell -NoProfile -Command '$root=$env:ADK_PROJECT_ROOT; if ($root) { if (-not [IO.Path]::IsPathRooted($root)) { exit 1 }; try { $root=(Resolve-Path -LiteralPath $root -ErrorAction Stop).Path } catch { exit 1 } } else { $root=git rev-parse --show-toplevel 2>$null; if ($LASTEXITCODE -ne 0 -or -not $root) { exit 1 }; $root=$root.Trim() }; if (-not (Test-Path -LiteralPath (Join-Path $root \".codex/hooks.json\"))) { exit 1 }; $registry=Join-Path $root.Trim() \".codex/hooks.json\"; if (-not (Test-Path -LiteralPath $registry)) { exit 0 }; $hook=Join-Path $root.Trim() \".codex/hooks/request-contract.cjs\"; if (-not (Test-Path -LiteralPath $hook)) { Write-Error \"Configured Codex hook is missing: $hook\"; exit 1 }; node $hook PreCompact'"
          }
        ]
      }
    ],
    "PostCompact": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "root=${ADK_PROJECT_ROOT:-}; if [ -n \"$root\" ]; then case \"$root\" in /*) ;; *) exit 1;; esac; root=$(CDPATH= cd -- \"$root\" 2>/dev/null && pwd -P) || exit 1; else root=$(git rev-parse --show-toplevel 2>/dev/null) || exit 1; fi; [ -f \"$root/.codex/hooks.json\" ] || exit 1; hook=\"$root/.codex/hooks/session-inject.cjs\"; [ ! -f \"$hook\" ] || node \"$hook\"",
            "commandWindows": "powershell -NoProfile -Command '$root=$env:ADK_PROJECT_ROOT; if ($root) { if (-not [IO.Path]::IsPathRooted($root)) { exit 1 }; try { $root=(Resolve-Path -LiteralPath $root -ErrorAction Stop).Path } catch { exit 1 } } else { $root=git rev-parse --show-toplevel 2>$null; if ($LASTEXITCODE -ne 0 -or -not $root) { exit 1 }; $root=$root.Trim() }; if (-not (Test-Path -LiteralPath (Join-Path $root \".codex/hooks.json\"))) { exit 1 }; $hook=Join-Path $root.Trim() \".codex/hooks/session-inject.cjs\"; if (-not (Test-Path -LiteralPath $hook)) { exit 0 }; node $hook'",
            "statusMessage": "Restoring ADK workflow state"
          },
          {
            "type": "command",
            "command": "root=${ADK_PROJECT_ROOT:-}; if [ -n \"$root\" ]; then case \"$root\" in /*) ;; *) exit 1;; esac; root=$(CDPATH= cd -- \"$root\" 2>/dev/null && pwd -P) || exit 1; else root=$(git rev-parse --show-toplevel 2>/dev/null) || exit 1; fi; [ -f \"$root/.codex/hooks.json\" ] || exit 1; registry=\"$root/.codex/hooks.json\"; [ ! -f \"$registry\" ] && exit 0; hook=\"$root/.codex/hooks/request-contract.cjs\"; if [ ! -f \"$hook\" ]; then echo \"Configured Codex hook is missing: $hook\" >&2; exit 1; fi; node \"$hook\" PostCompact",
            "commandWindows": "powershell -NoProfile -Command '$root=$env:ADK_PROJECT_ROOT; if ($root) { if (-not [IO.Path]::IsPathRooted($root)) { exit 1 }; try { $root=(Resolve-Path -LiteralPath $root -ErrorAction Stop).Path } catch { exit 1 } } else { $root=git rev-parse --show-toplevel 2>$null; if ($LASTEXITCODE -ne 0 -or -not $root) { exit 1 }; $root=$root.Trim() }; if (-not (Test-Path -LiteralPath (Join-Path $root \".codex/hooks.json\"))) { exit 1 }; $registry=Join-Path $root.Trim() \".codex/hooks.json\"; if (-not (Test-Path -LiteralPath $registry)) { exit 0 }; $hook=Join-Path $root.Trim() \".codex/hooks/request-contract.cjs\"; if (-not (Test-Path -LiteralPath $hook)) { Write-Error \"Configured Codex hook is missing: $hook\"; exit 1 }; node $hook PostCompact'"
          }
        ]
      }
    ],
    "Stop": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "root=${ADK_PROJECT_ROOT:-}; if [ -n \"$root\" ]; then case \"$root\" in /*) ;; *) exit 1;; esac; root=$(CDPATH= cd -- \"$root\" 2>/dev/null && pwd -P) || exit 1; else root=$(git rev-parse --show-toplevel 2>/dev/null) || exit 1; fi; [ -f \"$root/.codex/hooks.json\" ] || exit 1; registry=\"$root/.codex/hooks.json\"; [ ! -f \"$registry\" ] && exit 0; hook=\"$root/.codex/hooks/request-contract.cjs\"; if [ ! -f \"$hook\" ]; then echo \"Configured Codex hook is missing: $hook\" >&2; exit 1; fi; node \"$hook\" Stop",
            "commandWindows": "powershell -NoProfile -Command '$root=$env:ADK_PROJECT_ROOT; if ($root) { if (-not [IO.Path]::IsPathRooted($root)) { exit 1 }; try { $root=(Resolve-Path -LiteralPath $root -ErrorAction Stop).Path } catch { exit 1 } } else { $root=git rev-parse --show-toplevel 2>$null; if ($LASTEXITCODE -ne 0 -or -not $root) { exit 1 }; $root=$root.Trim() }; if (-not (Test-Path -LiteralPath (Join-Path $root \".codex/hooks.json\"))) { exit 1 }; $registry=Join-Path $root.Trim() \".codex/hooks.json\"; if (-not (Test-Path -LiteralPath $registry)) { exit 0 }; $hook=Join-Path $root.Trim() \".codex/hooks/request-contract.cjs\"; if (-not (Test-Path -LiteralPath $hook)) { Write-Error \"Configured Codex hook is missing: $hook\"; exit 1 }; node $hook Stop'"
          },
          {
            "type": "command",
            "command": "root=${ADK_PROJECT_ROOT:-}; if [ -n \"$root\" ]; then case \"$root\" in /*) ;; *) exit 1;; esac; root=$(CDPATH= cd -- \"$root\" 2>/dev/null && pwd -P) || exit 1; else root=$(git rev-parse --show-toplevel 2>/dev/null) || exit 1; fi; [ -f \"$root/.codex/hooks.json\" ] || exit 1; hook=\"$root/.claude/hooks/context-translation-batch.cjs\"; [ ! -f \"$hook\" ] || node \"$hook\" --flush",
            "commandWindows": "powershell -NoProfile -Command '$root=$env:ADK_PROJECT_ROOT; if ($root) { if (-not [IO.Path]::IsPathRooted($root)) { exit 1 }; try { $root=(Resolve-Path -LiteralPath $root -ErrorAction Stop).Path } catch { exit 1 } } else { $root=git rev-parse --show-toplevel 2>$null; if ($LASTEXITCODE -ne 0 -or -not $root) { exit 1 }; $root=$root.Trim() }; if (-not (Test-Path -LiteralPath (Join-Path $root \".codex/hooks.json\"))) { exit 1 }; $hook=Join-Path $root.Trim() \".claude/hooks/context-translation-batch.cjs\"; if (-not (Test-Path -LiteralPath $hook)) { exit 0 }; node $hook --flush'"
          }
        ]
      }
    ]
  }
}
