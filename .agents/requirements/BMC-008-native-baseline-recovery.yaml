id: BMC-008
title: "Native Platform Baseline and Recovery"
title_ko: "Native Platform Baseline and Recovery"
product: naia-benchmark-contract
module: platform-recovery
status: active
source: human
priority: high
created: 2026-07-29
updated: 2026-07-29
issue_url: "https://github.com/nextain/naia-adk/issues/16"
description: |
  A small immutable pre-recovery baseline must report only mapped observations, support rollback and missed-run recovery, and execute natively on Windows without WSL, Bash, or symlink requirements.
acceptance_criteria:
  - "The baseline manifest resolves exact task IDs, fixture and scorer digests, routes, controls, repetitions, denominator, and task-to-gate mappings before execution."
  - "Unobserved gates remain unavailable and cannot imply global GREEN or acceptance."
  - "The Windows runner and verifier use native Node or PowerShell paths with equivalent Linux and macOS adapters."
trace:
  code: [{ path: "packages/benchmark-contract/schemas/baseline-manifest.schema.json", symbol: "resolved baseline task, route, control, scorer digest, and native-platform contract", coverage: full }, { path: "packages/benchmark-contract/src/validate-bundle.mjs", symbol: "baseline slot, scorer, gate mapping, denominator, and native-Windows validation", coverage: full }, { path: "packages/benchmark-contract/baselines/pre-recovery.manifest.json", symbol: "immutable 12-task baseline and exact gate partition", coverage: full }, { path: "packages/benchmark-contract/src/native-runner.mjs", symbol: "frozen plan, external-key journal, durable observation, exact resume, portable adapter boundary, and native Node CLI", coverage: full }]
  tests: [{ path: "packages/benchmark-contract/test/validate-benchmark-contract.mjs", symbol: "bundle gate mapping and no-WSL fixtures", coverage: full }, { path: "packages/benchmark-contract/test/validate-pre-recovery-baseline.mjs", symbol: "external anchors, exact denominator, route resolution, scorer bytes, gate partition, and native Windows negatives", coverage: full }, { path: "packages/benchmark-contract/test/native-runner.test.mjs", symbol: "plan/run/resume, external integrity authority, crash recovery, cost aggregation, lock races, adapter mutation, and Windows CLI", coverage: full }]
  reviews: { planning: ["NAIA-BENCH-CONTRACT-001 sequences 2 and 3"], development: [".agents/reviews/benchmark-baseline-2eb67316/reviewer-1.json", ".agents/reviews/benchmark-baseline-2eb67316/reviewer-2.json", ".agents/reviews/benchmark-runner-09de8e98/reviewer-1.json", ".agents/reviews/benchmark-runner-09de8e98/reviewer-2.json"], test: null, integration: null }
decisions: ["NAIA-BENCH-CONTRACT-001"]
