<!-- RDD Insight Ledger — APPEND-ONLY institutional memory. Never edit/delete prior entries.
     Searched at recall (rdd-audit G4 tag-recall). Record EVERY experiment/decision, especially
     failures. Each entry: "## <experiment_id> — <date>" + a "tags: [..]" line (machine-parsed). -->

# Insight Ledger — <program-name>

## H0-example — 2026-01-01
tags: [example, template]
- what: <what was actually done — the method that ran>
- result_vs_gate: <PASS/REFUTE against the PRE-REGISTERED gate>
- insight: <the durable lesson. e.g. "집계지표(formant 7/7) ≠ 지각(사람은 언어로 안 들림)">
